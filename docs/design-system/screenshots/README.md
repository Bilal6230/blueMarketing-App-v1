# Screenshot Capture

Automated screenshot capture was not available in this environment on Thursday, July 16, 2026.

Manual capture routes:

- `/(auth)/login` for Login foundation, light theme
- `/(preview)/login` for Login preview, dark theme
- `/(preview)/staff-home` for Staff home, light theme
- `/(preview)/admin-home` for Admin home, dark theme
- `/(preview)/crm` for CRM loaded state and CRM offline state
- `/(preview)/lead-detail` for Lead detail
- `/(preview)/attendance` for Attendance checked-in and checked-out
- `/(preview)/design-system` for the design-system showcase
