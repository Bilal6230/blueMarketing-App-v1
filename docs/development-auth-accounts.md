# Temporary frontend auth accounts

These accounts are for the local frontend-only auth service in `src/features/auth/services/mockAuthService.ts`.

- Staff: `staff@bluemarketing.com` / `password123`
- Administrator: `admin@bluemarketing.com` / `password123`
